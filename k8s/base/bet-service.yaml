apiVersion: apps/v1
kind: Deployment
metadata:
  name: bet-service
spec:
  selector:
    matchLabels:
      app: bet-service
  template:
    metadata:
      labels:
        app: bet-service
    spec:
      containers:
      - name: bet-service
        image: sportsbook-bet-service:latest
        command: ["/app/bet-service"]
        envFrom:
        - configMapRef:
            name: sportsbook-config
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bet-service
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: bet-service
