apiVersion: apps/v1
kind: Deployment
metadata:
  name: odds-service
spec:
  selector:
    matchLabels:
      app: odds-service
  template:
    metadata:
      labels:
        app: odds-service
    spec:
      containers:
      - name: odds-service
        image: sportsbook-odds-service:latest
        command: ["/app/odds-service"]
        envFrom:
        - configMapRef:
            name: sportsbook-config
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: odds-service
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: odds-service
