apiVersion: apps/v1
kind: Deployment
metadata:
  name: settlement-service
spec:
  selector:
    matchLabels:
      app: settlement-service
  template:
    metadata:
      labels:
        app: settlement-service
    spec:
      containers:
      - name: settlement-service
        image: sportsbook-settlement-service:latest
        command: ["/app/settlement-service"]
        envFrom:
        - configMapRef:
            name: sportsbook-config
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: settlement-service
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: settlement-service
