apiVersion: apps/v1
kind: Deployment
metadata:
  name: bet-worker
spec:
  selector:
    matchLabels:
      app: bet-worker
  template:
    metadata:
      labels:
        app: bet-worker
    spec:
      containers:
      - name: bet-worker
        image: sportsbook-bet-worker:latest
        command: ["/app/bet-worker"]
        envFrom:
        - configMapRef:
            name: sportsbook-config
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 10
        readinessProbe:
          httpGet:
            path: /healthz
            port: 8080
          initialDelaySeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: bet-worker
spec:
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: bet-worker
